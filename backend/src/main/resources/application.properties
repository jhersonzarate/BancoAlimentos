# ============================================================
# application.properties — Banco de Alimentos Perú
# ============================================================

# Servidor
server.port=8080

# Base de datos Neon PostgreSQL
spring.datasource.url=jdbc:postgresql://ep-rough-brook-acx5atc3.sa-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_WhV4DQc5svaG
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Nombre de la app
spring.application.name=gestion-donaciones

# CORS — permite peticiones desde Angular (dev)
# Se configura también en WebConfig.java